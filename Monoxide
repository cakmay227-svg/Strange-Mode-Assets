---====== Load spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/V2/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.Create({
	Entity = {
		Name = "Monoxide",
		Asset = "91836550237811",
		HeightOffset = 0
	},
	Lights = {
		Flicker = {
			Enabled = false,
			Duration = 4
		},
		Shatter = true,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = true,
		Range = 200,
		Values = {30, 20, 1, 0.1} -- Magnitude, Roughness, FadeIn, FadeOut
	},
	Movement = {
		Speed = 310,
		Delay = 7,
		Reversed = false
	},
	Rebounding = {
		Enabled = true,
		Type = "Ambush", -- "Blitz"
		Min = 10,
		Max = 11,
		Delay = 0
	},
	Damage = {
		Enabled = true,
		Range = 40,
		Amount = 125
	},
	Crucifixion = {
		Enabled = false,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding", -- "Curious"
		Hints = {"Death", "Hints", "Go", "Here"},
		Cause = "Monoxide"
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
	print("Entity has spawned")
	
	task.spawn(function() 
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")

local camera = workspace.CurrentCamera

-- EFFECT
local cc = Instance.new("ColorCorrectionEffect")
cc.Name = "HeatbiteEffect"

cc.TintColor = Color3.fromRGB(0,0,255)
cc.Saturation = 0.3
cc.Contrast = 0.15
cc.Brightness = 0.08

cc.Parent = Lighting

require(game.Players.LocalPlayer.PlayerGui.MainUI.Initiator.Main_Game).caption("Monoxide: Im here",true)

-- SHAKE
local shaking = true
local originalCF = camera.CFrame

local connection
connection = RunService.RenderStepped:Connect(function()
	if not shaking then
		return
	end

	local offset = Vector3.new(
		(math.random() - 0.5) * 0.08,
		(math.random() - 0.5) * 0.08,
		(math.random() - 0.5) * 0.02
	)

	camera.CFrame = camera.CFrame * CFrame.new(offset)
end)

-- FADE OUT
local tween = TweenService:Create(
	cc,
	TweenInfo.new(
		3,
		Enum.EasingStyle.Linear
	),
	{
		Saturation = 0,
		Contrast = 0,
		Brightness = 0,
		TintColor = Color3.new(1,1,1)
	}
)

tween:Play()

tween.Completed:Connect(function()
	shaking = false

	if connection then
		connection:Disconnect()
	end

	cc:Destroy()
end)
end) 
end)

entity:SetCallback("OnStartMoving", function()
    print("Entity has started moving")
end)

entity:SetCallback("OnEnterRoom", function(room: Model, firstTime: boolean)
    if firstTime then
        print("Entity has entered room: "..room.Name.." for the first time")
    else
        print("Entity has entered room: "..room.Name.." again")
    end
end)

entity:SetCallback("OnLookAt", function(lineOfSight: boolean)
	if lineOfSight == true then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound: boolean)
    if startOfRebound == true then
        print("Entity has started rebounding")
	else
        print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
    print("Entity is despawning")
end)

entity:SetCallback("OnDespawned", function()
    print("Entity has despawned")
    if getgenv()._MONOXIDE_ALREADY_EXECUTED then
    return
end

getgenv()._MONOXIDE_ALREADY_EXECUTED = true

local achievementGiver = loadstring(game:HttpGet("https://raw.githubusercontent.com/Voor-Pr00/Achivements/refs/heads/main/Voorpr0"))()

---====== Display achievement ======---
achievementGiver({
    Title = "Silent Killer",
    Desc ="Invisible, odorless, inevitable.",
    Reason = "Encountered Monoxide",
    Image = "rbxassetid://130431052698777"
})
end)

entity:SetCallback("OnDamagePlayer", function(newHealth: number)
	if newHealth <= 0 then
		print("Entity has killed the player")
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local guiParent = player:WaitForChild("PlayerGui")

-- GUI ROOT
local gui = Instance.new("ScreenGui")
gui.Name = "ULTRA_JUMPSCARE"
gui.IgnoreGuiInset = true
gui.ResetOnSpawn = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Global
gui.Parent = guiParent

-- BLACK BASE
local black = Instance.new("Frame")
black.Size = UDim2.new(1,0,1,0)
black.BackgroundColor3 = Color3.fromRGB(0,0,0)
black.BackgroundTransparency = 1
black.ZIndex = 1
black.Parent = gui

-- RED LAYER
local red = Instance.new("Frame")
red.Size = UDim2.new(1,0,1,0)
red.BackgroundColor3 = Color3.fromRGB(0,0,255)
red.BackgroundTransparency = 1
red.ZIndex = 2
red.Parent = gui

-- NOISE
local noise = Instance.new("ImageLabel")
noise.Size = UDim2.new(1,0,1,0)
noise.BackgroundTransparency = 1
noise.Image = "rbxassetid://9118823103"
noise.ImageTransparency = 1
noise.ScaleType = Enum.ScaleType.Stretch
noise.ZIndex = 3
noise.Parent = gui

-- VIGNETTE
local vignette = Instance.new("ImageLabel")
vignette.Size = UDim2.new(1.2,0,1.2,0)
vignette.Position = UDim2.new(-0.1,0,-0.1,0)
vignette.BackgroundTransparency = 1
vignette.Image = "rbxassetid://9183863670"
vignette.ImageTransparency = 1
vignette.ZIndex = 4
vignette.Parent = gui

-- FLASH
local flash = Instance.new("Frame")
flash.Size = UDim2.new(1,0,1,0)
flash.BackgroundColor3 = Color3.fromRGB(255,255,255)
flash.BackgroundTransparency = 1
flash.ZIndex = 10
flash.Parent = gui

-- ENTITY IMAGE
local scare = Instance.new("ImageLabel")
scare.AnchorPoint = Vector2.new(0.5,0.5)
scare.Position = UDim2.new(0.5,0,0.5,0)
scare.Size = UDim2.new(0.01,0,0.01,0)
scare.BackgroundTransparency = 1
scare.Image = "rbxassetid://100904116732941"
scare.ImageTransparency = 1
scare.ScaleType = Enum.ScaleType.Fit
scare.ZIndex = 20
scare.Parent = gui

-- SOUND
local sound = Instance.new("Sound")
sound.SoundId = "rbxassetid://140582989782464"
sound.Volume = 5
sound.Parent = gui

--------------------------------------------------
-- SCREEN SHAKE
--------------------------------------------------

local function shake(duration,power)

	local start = tick()
	local connection

	connection = RunService.RenderStepped:Connect(function()

		if tick() - start >= duration then
			gui.Position = UDim2.new()
			connection:Disconnect()
			return
		end

		gui.Position = UDim2.new(
			0,
			math.random(-power,power),
			0,
			math.random(-power,power)
		)
	end)
end

--------------------------------------------------
-- IMAGE SHAKE
--------------------------------------------------

local function imageShake(duration,power)

	local start = tick()
	local connection

	connection = RunService.RenderStepped:Connect(function()

		if tick() - start >= duration then
			scare.Position = UDim2.new(0.5,0,0.5,0)
			connection:Disconnect()
			return
		end

		scare.Position = UDim2.new(
			0.5,
			math.random(-power,power),
			0.5,
			math.random(-power,power)
		)
	end)
end

--------------------------------------------------
-- JUMPSCARE
--------------------------------------------------

local function redJumpscare()

	sound:Play()

	-- FLASH
	flash.BackgroundTransparency = 0

	TweenService:Create(
		flash,
		TweenInfo.new(0.1),
		{
			BackgroundTransparency = 1
		}
	):Play()

	-- RED
	TweenService:Create(
		red,
		TweenInfo.new(0.15),
		{
			BackgroundTransparency = 0.4
		}
	):Play()

	-- NOISE
	TweenService:Create(
		noise,
		TweenInfo.new(0.1),
		{
			ImageTransparency = 0.2
		}
	):Play()

	-- VIGNETTE
	TweenService:Create(
		vignette,
		TweenInfo.new(0.15),
		{
			ImageTransparency = 0.3
		}
	):Play()

	--------------------------------------------------
	-- ENTITY APPEARS FAR AWAY
	--------------------------------------------------

	scare.ImageTransparency = 0
	scare.Size = UDim2.new(0.02,0,0.02,0)
	scare.Position = UDim2.new(0.5,0,0.5,0)

	imageShake(0.25,20)

	local zoomTween = TweenService:Create(
		scare,
		TweenInfo.new(
			0.8,
			Enum.EasingStyle.Exponential,
			Enum.EasingDirection.Out
		),
		{
			Size = UDim2.new(3.5,0,3.5,0)
		}
	)

	zoomTween:Play()

	--------------------------------------------------
	-- SCREEN SHAKE
	--------------------------------------------------

	shake(0.7,25)

	--------------------------------------------------
	-- FLASH STROBE
	--------------------------------------------------

	task.spawn(function()

		for i = 1,8 do

			flash.BackgroundTransparency = 0

			task.wait(0.02)

			flash.BackgroundTransparency = 1

			task.wait(0.04)
		end
	end)

	--------------------------------------------------
	-- NOISE GLITCH
	--------------------------------------------------

	task.spawn(function()

		for i = 1,15 do

			noise.ImageTransparency =
				math.random(10,35)/100

			task.wait(0.03)
		end
	end)

	task.wait(0.9)

	--------------------------------------------------
	-- FADE OUT
	--------------------------------------------------

	TweenService:Create(
		scare,
		TweenInfo.new(0.5),
		{
			ImageTransparency = 1
		}
	):Play()

	TweenService:Create(
		noise,
		TweenInfo.new(0.5),
		{
			ImageTransparency = 1
		}
	):Play()

	TweenService:Create(
		red,
		TweenInfo.new(0.5),
		{
			BackgroundTransparency = 1
		}
	):Play()

	TweenService:Create(
		vignette,
		TweenInfo.new(0.5),
		{
			ImageTransparency = 1
		}
	):Play()
	
	game:Shutdown()

	task.wait(0.6)

	gui:Destroy()
end

redJumpscare()
	else
		print("Entity has damaged the player")
	end
end)

--[[

DEVELOPER NOTE:
By overwriting 'CrucifixionOverwrite' the default crucifixion callback will be replaced with your custom callback.

entity:SetCallback("CrucifixionOverwrite", function()
    print("Custom crucifixion callback")
end)

]]--

---====== Run entity ======---

entity:Run(true)
